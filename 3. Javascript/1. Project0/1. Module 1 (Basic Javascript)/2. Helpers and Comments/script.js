document.write("hello world");
alert("hello people");
console.log("hello me");

/*These are called helpers, meaning they allow us to check out our code multiple times  across our coding.
Out of these console.log(); is the most powerful, and most used by programmers, because it can also show us objects, meaning the key value pairs. while in alert(); and document.write(); it will only output "Object:object". */
// Also observe this is the other method of doing the comments in javascript. by using the /* */ to end the comment along with the // which only comments the line.
